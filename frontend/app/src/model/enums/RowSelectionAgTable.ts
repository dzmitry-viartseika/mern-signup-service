/**
 * Параметры позволяющие выбирать в таблице несколько рядов
 * */

enum RowSelectionAgTable {
  /** Только один ряд */
  single = 'single',

  /** Множественный выбор рядов */
  multiple = 'multiple',
}

export default RowSelectionAgTable;
